const { resolve } = require('path') // padronizar o caminho

module.exports  = {
    config: resolve(__dirname, 'src', 'config', 'database.js'), // caminho  configuração db
    'models-path': resolve(__dirname, 'src', 'app', 'models'), // caminho para local onde encontra os models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds')
}
